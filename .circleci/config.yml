version: 2.1
orbs:
  convox: convox/orb@1.4.1
workflows:
  deploy:
    jobs:
      - deploy
jobs:
  deploy:
    executor: convox/cli
    steps:
      - checkout
      - convox/deploy:
          rack: cgdemo/do-test
          app: nodedemo
          host: console.convox.com